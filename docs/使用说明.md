# 📖 使用说明

## ✅ 完成情况

### 1. 成员B模块整理 ✅

**回答你的问题**：**成员B不需要依赖ProPainter！**

- ✅ 已整理到独立文件夹 `member_b/`
- ✅ 不依赖ProPainter核心代码
- ✅ 只依赖SAM和CUTIE追踪器

### 2. 代码解读文档 ✅

**详细代码解读**：`member_b/CODE_EXPLANATION.md`

文档包含：
- ✅ 每个函数的功能说明
- ✅ 代码流程解析
- ✅ 关键技术点解释
- ✅ 使用示例

### 3. 成员A模块创建 ✅

**文本输入→边界框**：`member_a/text_to_bbox.py`

- ✅ 使用GroundingDINO实现
- ✅ 输入文本，输出边界框
- ✅ 框架已搭建

## 📁 文件结构

```
ProPainter/
├── member_a/                    # 成员A：文本定位 ⭐ 新创建
│   ├── __init__.py
│   ├── text_to_bbox.py         # 文本→边界框
│   └── README.md
│
├── member_b/                    # 成员B：分割与追踪 ⭐ 已整理
│   ├── __init__.py
│   ├── segment_track.py         # 核心模块
│   ├── api.py                   # API接口
│   ├── README.md
│   └── CODE_EXPLANATION.md      # 代码解读文档 ⭐
│
└── (其他文件...)
```

## 🚀 如何使用

### 成员B：分割与追踪

```python
from member_b import MemberBTrackAnything

# 初始化
module = MemberBTrackAnything()

# 处理视频
bboxes = [[100, 100, 200, 200]]  # 来自成员A
mask_path, masks = module.process_video(
    video_path='video.mp4',
    bboxes=bboxes
)

print(f"掩码保存在: {mask_path}")
```

### 成员A：文本→边界框

```python
from member_a import TextToBBox

# 初始化
module = TextToBBox()

# 处理视频
bboxes = module.process_video_first_frame(
    video_path='video.mp4',
    text_prompt='car'  # 或 "删除视频中的汽车"
)

print(f"检测到 {len(bboxes)} 个目标物体")
```

### 完整流程

```python
# 1. 成员A：文本→边界框
from member_a import TextToBBox
module_a = TextToBBox()
bboxes = module_a.process_video_first_frame(
    video_path='inputs/object_removal/bmx-trees',
    text_prompt='car'
)

# 2. 成员B：边界框→掩码
from member_b import MemberBTrackAnything
module_b = MemberBTrackAnything()
mask_path, masks = module_b.process_video(
    video_path='inputs/object_removal/bmx-trees',
    bboxes=bboxes
)

# 3. 成员C：掩码→修复视频
# python inference_propainter.py -i video.mp4 -m mask_path -o results/inpainted
```

## 📖 代码解读

**查看详细代码解读**：`member_b/CODE_EXPLANATION.md`

文档包含：
- 每个函数的功能说明
- 代码流程解析
- 关键技术点解释
- 使用示例

## 📝 输入输出

### 输入输出流程

```
文本输入（成员A）
    ↓ "删除视频中的汽车"
边界框 [[x1,y1,x2,y2]]（成员A）
    ↓
掩码序列（成员B）⭐
    ↓
修复视频（成员C）
```

### 成员B的输入输出

**输入**：
- 视频路径
- 边界框列表：`[[x1, y1, x2, y2], ...]`

**输出**：
- 掩码文件夹路径
- 掩码序列（PNG文件）

## ✅ 总结

1. ✅ **成员B已整理到独立文件夹** `member_b/`
2. ✅ **不依赖ProPainter核心代码**
3. ✅ **代码解读文档已创建** `member_b/CODE_EXPLANATION.md`
4. ✅ **成员A模块已创建** `member_a/`（文本→边界框）

现在你可以：
- 查看 `member_b/CODE_EXPLANATION.md` 了解代码如何工作
- 使用 `member_b/` 中的模块（独立于ProPainter）
- 使用 `member_a/` 实现文本到边界框的转换

